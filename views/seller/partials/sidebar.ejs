<aside
  class="fixed top-0 left-0 z-40 w-72 h-screen bg-slate-900 text-white flex flex-col border-r border-slate-800"
>
  <!-- ===================================================== -->
  <!-- LOGO / BRAND -->
  <!-- ===================================================== -->

  <div class="h-24 px-6 flex items-center border-b border-slate-800">
    <div
      class="w-12 h-12 rounded-xl bg-indigo-600 flex items-center justify-center shadow-lg shadow-indigo-600/20"
    >
      <span class="text-xl font-bold text-white"> P </span>
    </div>

    <div class="ml-4">
      <h1 class="text-lg font-bold text-white">POÈME</h1>

      <p class="text-xs text-slate-400 tracking-wider">SELLER PANEL</p>
    </div>
  </div>

  <!-- ===================================================== -->
  <!-- NAVIGATION -->
  <!-- ===================================================== -->

  <nav class="flex-1 px-3 py-6 overflow-y-auto">
    <!-- Dashboard -->

    <a
      href="/poeme-perfumery/seller/dashboard"
      class="group flex items-center gap-4 px-5 py-3.5 mb-2 rounded-xl transition-all duration-200 <%= currentPage === 'dashboard' ? 'bg-indigo-600 text-white shadow-lg shadow-indigo-600/20' : 'text-slate-300 hover:bg-slate-800 hover:text-white' %>"
    >
      <i
        class="fa-solid fa-chart-line w-5 text-center <%= currentPage === 'dashboard' ? 'text-white' : 'text-slate-400 group-hover:text-white' %>"
      ></i>

      <span class="font-medium"> Dashboard </span>
    </a>

    <!-- ================================================= -->
    <!-- PRODUCTS -->
    <!-- ================================================= -->

    <a
      href="/poeme-perfumery/seller/products"
      class="group flex items-center gap-4 px-5 py-3.5 mb-2 rounded-xl transition-all duration-200 <%= currentPage === 'products' ? 'bg-indigo-600 text-white shadow-lg shadow-indigo-600/20' : 'text-slate-300 hover:bg-slate-800 hover:text-white' %>"
    >
      <i
        class="fa-solid fa-box w-5 text-center <%= currentPage === 'products' ? 'text-white' : 'text-slate-400 group-hover:text-white' %>"
      ></i>

      <span class="font-medium"> Products </span>
    </a>

    <!-- ================================================= -->
    <!-- CATEGORIES -->
    <!-- ================================================= -->

    <a
      href="/poeme-perfumery/seller/categories"
      class="group flex items-center gap-4 px-5 py-3.5 mb-2 rounded-xl transition-all duration-200 <%= currentPage === 'categories' ? 'bg-indigo-600 text-white shadow-lg shadow-indigo-600/20' : 'text-slate-300 hover:bg-slate-800 hover:text-white' %>"
    >
      <i
        class="fa-solid fa-layer-group w-5 text-center <%= currentPage === 'categories' ? 'text-white' : 'text-slate-400 group-hover:text-white' %>"
      ></i>

      <span class="font-medium"> Categories </span>
    </a>

    <!-- ================================================= -->
    <!-- ORDERS -->
    <!-- ================================================= -->

    <a
      href="/poeme-perfumery/seller/orders"
      class="group flex items-center gap-4 px-5 py-3.5 mb-2 rounded-xl transition-all duration-200 <%= currentPage === 'orders' ? 'bg-indigo-600 text-white shadow-lg shadow-indigo-600/20' : 'text-slate-300 hover:bg-slate-800 hover:text-white' %>"
    >
      <i
        class="fa-solid fa-cart-shopping w-5 text-center <%= currentPage === 'orders' ? 'text-white' : 'text-slate-400 group-hover:text-white' %>"
      ></i>

      <span class="font-medium"> Orders </span>
    </a>

    <!-- ================================================= -->
    <!-- IMAGE LIBRARY -->
    <!-- ================================================= -->

    <a
      href="/poeme-perfumery/seller/image-library"
      class="group flex items-center gap-4 px-5 py-3.5 mb-2 rounded-xl transition-all duration-200 <%= currentPage === 'image-library' ? 'bg-indigo-600 text-white shadow-lg shadow-indigo-600/20' : 'text-slate-300 hover:bg-slate-800 hover:text-white' %>"
    >
      <i
        class="fa-solid fa-images w-5 text-center <%= currentPage === 'image-library' ? 'text-white' : 'text-slate-400 group-hover:text-white' %>"
      ></i>

      <span class="font-medium"> Image Library </span>
    </a>

    <!-- ================================================= -->
    <!-- CHAT SUPPORT -->
    <!-- ================================================= -->

    <a
      href="/poeme-perfumery/seller/chat"
      class="group flex items-center gap-4 px-5 py-3.5 mb-2 rounded-xl transition-all duration-200 <%= currentPage === 'chat' ? 'bg-indigo-600 text-white shadow-lg shadow-indigo-600/20' : 'text-slate-300 hover:bg-slate-800 hover:text-white' %>"
    >
      <i
        class="fa-solid fa-comments w-5 text-center <%= currentPage === 'chat' ? 'text-white' : 'text-slate-400 group-hover:text-white' %>"
      ></i>

      <span class="font-medium"> Chat Support </span>
    </a>

    <!-- ================================================= -->
    <!-- SETTINGS -->
    <!-- ================================================= -->

    <a
      href="/poeme-perfumery/seller/settings"
      class="group flex items-center gap-4 px-5 py-3.5 mb-2 rounded-xl transition-all duration-200 <%= currentPage === 'settings' ? 'bg-indigo-600 text-white shadow-lg shadow-indigo-600/20' : 'text-slate-300 hover:bg-slate-800 hover:text-white' %>"
    >
      <i
        class="fa-solid fa-gear w-5 text-center <%= currentPage === 'settings' ? 'text-white' : 'text-slate-400 group-hover:text-white' %>"
      ></i>

      <span class="font-medium"> Settings </span>
    </a>
  </nav>

  <!-- ===================================================== -->
  <!-- LOGOUT -->
  <!-- ===================================================== -->

  <div class="p-5 border-t border-slate-800">
    <a
      href="/poeme-perfumery/seller/logout"
      class="group flex items-center gap-4 px-5 py-3.5 rounded-xl text-red-400 hover:bg-red-600 hover:text-white transition-all duration-200"
    >
      <!-- Logout Icon -->

      <div
        class="w-9 h-9 rounded-lg bg-red-500/10 group-hover:bg-white/20 flex items-center justify-center transition-all duration-200"
      >
        <i class="fa-solid fa-right-from-bracket"></i>
      </div>

      <!-- Logout Text -->

      <span class="font-medium"> Logout </span>
    </a>
  </div>
</aside>
